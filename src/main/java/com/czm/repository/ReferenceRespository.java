package com.czm.repository;

import com.czm.model.ReferenceEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReferenceRespository extends JpaRepository<ReferenceEntity,Integer> {
}
